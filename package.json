{
  "name": "lymlyn",
  "version": "1.0.0",
  "description": "LYM|LYN E-Commerce Platform",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"frontend,admin,backend\" -c \"cyan,magenta,yellow\" \"npm run dev --prefix frontend\" \"npm run dev --prefix admin\" \"npm run dev --prefix backend\"",
    "install:all": "npm install --prefix frontend && npm install --prefix admin && npm install --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
